<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../components/head') %>
        <link rel="stylesheet" href="../style/dist/index.css">
</head>

<body>
    <% if (mainPage) { %>
        <%- include('../components/navbar', {mainPage}) %>

            <div>
                <img src="<%- mainPage.mainPics%>" alt="mainPage">
                <p>
                    <%- mainPage.desc%>
                </p>
            </div>
            <% } %>

                <% if (places) { %>
                    <% places.forEach(place=> { %>
                        <div>
                            <%- include ('../components/place.ejs', {place}) %>
                        </div>
                        <% }) %>
                            <% } %>

                                <%- include('../components/footer') %>
</body>

</html>