<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../components/head') %>
</head>

<body>
    <h1>THIS IS HOME PAGEEEEE!!!!!!!111</h1>

    <% if (places) { %>
        <% places.forEach(place=> { %>
            <div>
                <%- include ('../components/place.ejs', {place}) %>
            </div>
            <% }) %>
                <% } %>
</body>

</html>